package com.xiaocai.app

class Size(var width: Int, var height: Int, var interval: Int, var intervals: String) {

    override fun toString(): String {
        return "width=$width, height=$height, interval=$interval, intervals=$intervals"
    }
}